1)throwable
error (ошибка JVM) ошибка утечки памяти
exception
checked исключения (обязательно необходимо перехватывать) не скомпилируется
unchecked исключения (обязательно необходимо перехватывать)

2) try  {} catch  {ArithmeticException e} методы отлавливают ошибку
3) try {}
    catch (ArithmeticException e) {}
    сatch (ArrayIndexOutOfBoundsException e) {  }
!! Сначала подклассы исключений, потом базовый
4) throw в теле метода исключение вызывается
    throw new ArithmeticException("нельзя отрицательное!")
     Сами обрабатываем исключение. Зачем исключения,
    что бы не делал специальные флаги, а работать с ошибками
5) throws при описании метода вызывается.
 Обязывает создавать исключение
public static void createReport() throws IOException
7) можно создавать свои классы ошибок, он детализирует

6) finally, в нем нет типа исключения, он отрабатывает в любом случае, если даже catch сработал

Базовый класс
+ можно перехватить любую ошибку, которая возникла
- нет детализации, где-то на этой странице возникла ошибка
Подклассы
+ программа падает, если возникает другая ошибка
- обычно делают от меньшего к большему